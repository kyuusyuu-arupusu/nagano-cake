<div class="container">
  <div class="row">
    <div class="col-xs-4">
      <h2>ショッピングカート</h2>
    </div>
    <div class="col-xs-4 offset-xs-4">
      <%= link_to "カートを空にする", customers_cart_items_path, method: :delete, "data-confirm" => "本当に削除しますか?", class: "btn btn-danger" %>
  </div>
  <div class="row">
  	<div class="col-xs-8">
  	  <table class="table table-bordered">
  	  	<thead>
  	  	  <tr>
  	  	  	<th>商品名</th>
  	  	  	<th>単価（税込)</th>
  	  	  	<th>数量</th>
  	  	  	<th>小計</th>
            <th></th>
  	  	  </tr>
  	  	</thead>
        <tbody>
        <% @cart_items.each do |cart_item| %>
          <tr>
            <td>
              <%= attachment_image_tag cart_item.item, :image, :fill, 20,20, format: "jpeg" %>
              <%= cart_item.item.name %>
            </td>
            <td><%= cart_item.item.price %></td>
            <td><%= cart_item.quantity %></td>
            <td><%= cart_item.item.price * cart_item.quantity  %></td>
            <td><%= link_to "削除する", "/customers/cart_items/#{cart_item.id}", method: :delete, class: "btn btn-danger" %></td>
          </tr>
        <% end %>
  	  </table>
  	</div>
  </div>
  <div class="row">
    <div class="col-xs-3">
      <%= link_to "編集する", root_path, class: "btn btn-info" %>
    </div>
    <div class="col-xs-5">
      <table class="table table-bordered">
        <tr>
          <td>合計金額</td>
          <td></td>
        </tr>
      </table>
    </div>
    <div class="row">
      <div class="col-xs-4 offset-4">
        <%= link_to "情報入力に進む", 
    </div>
</div>